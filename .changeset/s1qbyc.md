---
"vercel-api-js": patch
---

Reordered fields 'created', 'confirmed', 'confirmedAt', 'accessRequestedAt', and 'role' in userEventSchema and corresponding UserEvent type to match new schema structure.