---
"vercel-api-js": patch
---

Simplified buildMachine configuration for team/user resourceConfig objects; only the 'default' property is now recorded, and other per-build fields were removed from team/user schema and types.