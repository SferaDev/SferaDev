---
"netlify-api": minor
---

Add endpoints and types for managing database compute settings at the branch and project level, including get, set, and clear operations, and corresponding settings types.